use ceramic::{Ceramic, CeramicConfig};

fn main() {
    // Create a new Ceramic instance with default configuration
    let config = CeramicConfig::default();
    let ceramic = Ceramic::new(config);

    // Use the ceramic instance...
}
